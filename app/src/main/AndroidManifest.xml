<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bjutsoft.studywithme">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/main_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".About" />
        <activity android:name=".NotePad.EditNote" />
        <activity android:name=".NotePad.NewNote" />
        <activity android:name=".NotePad.NotePad" />
        <activity android:name=".AlarmClock.AlarmToast" />
        <activity android:name=".AlarmClock.AlarmClock" />
        <activity android:name=".Surfer.SurfOnline" />
        <activity android:name=".SoundMeter.SoundMeter" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>